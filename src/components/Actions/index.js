import { StyleSheet, Text, View } from "react-native";

export default function Actions() {
  return (
    <View>
      <Text>Eu sou componente de ações</Text>
    </View>
  );
}

const styles = StyleSheet.create({});
